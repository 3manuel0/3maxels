## Graphic library in C (wasm)

library that generates (RGBA) pixels, to create or display some basic shapes and hopefully more advanced ones, basically it's a software rendering library using wasm and js, for now only has (Rectangles and a small shader test).
